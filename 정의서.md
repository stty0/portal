# Slurm HPC Portal 기능 정의서

- 문서 버전: v0.1 (Draft)
- 작성일: 2026-07-15
- 대상 시스템: Slurm 기반 HPC 클러스터 웹 포털
- 참조 벤치마크: Open OnDemand, Altair Access(HPCWorks)

---

## 1. 개요

### 1.1 목적
연구자/엔지니어가 CLI 지식 없이 웹 브라우저만으로 Slurm 클러스터의 Job 제출·모니터링·파일 관리·인터랙티브 앱 사용이 가능하도록 하고, 관리자가 클러스터 자원·사용자·정책을 통합 관리할 수 있는 포털을 구축한다.

### 1.2 사용자 역할(Role) 정의

| 역할 | 코드 | 설명 |
|---|---|---|
| 일반 사용자 | USER | Job 제출/조회, 본인 파일·세션 관리, 본인 사용량 조회 |
| 관리자 | ADMIN | 클러스터/노드/파티션 관리, 전체 Job 제어, 사용자·계정·QOS 관리, 통계/감사 |

> 인증(authN)은 AD로 처리하되, 역할(authZ)은 포털이 관리한다 — AD 그룹/스키마를 변경하지 않고 포털 authZ 테이블(`ad_id → role`)에 role을 저장한다. AD와 조인하지 않으며(로그인 시 받은 AD 식별자를 키로 lookup), AD를 미러링·동기화하지 않는다.
> 필요 시 확장 역할: 그룹장(PI) — 소속 그룹 사용량·Job 조회 권한 (v2 검토)

---

## 2. 일반 사용자(USER) 기능 정의

### 2.1 클러스터

> 대시보드 기능은 관리자(ADMIN) 전용으로 제공하며, 일반 사용자에게는 클러스터 현황 화면을 제공한다.

| ID | 기능명 | 설명 | 관련 Slurm 명령/API | 우선순위 |
|---|---|---|---|---|
| U-CL-01 | 멀티 클러스터 현황 | 클러스터별 상태(정상/점검), 노드 수, idle 노드, GPU 가용량 요약 | sinfo -M | 필수 |
| U-CL-02 | 클러스터 가용 자원 | 파티션별 idle/alloc 노드, CPU/GPU/메모리 가용량 | sinfo | 필수 |
| U-CL-03 | 공지사항 | 관리자 등록 공지(점검, 정책 변경) 표시 | Portal DB | 필수 |

### 2.2 Job 관리

| ID | 기능명 | 설명 | 관련 Slurm 명령/API | 우선순위 |
|---|---|---|---|---|
| U-JB-01 | Job 제출(폼 기반) | 파티션, 노드/CPU/GPU 수, 메모리, walltime, 계정 선택 폼 → 배치 스크립트 자동 생성 후 제출 | sbatch | 필수 |
| U-JB-02 | Job 제출(스크립트) | 배치 스크립트 직접 작성/업로드 후 제출, 웹 에디터 제공 | sbatch | 필수 |
| U-JB-03 | Job 템플릿 | 앱별(예: Python, MPI, GROMACS) 사전 정의 템플릿 선택 → 파라미터만 입력 | sbatch + 템플릿 엔진 | 필수 |
| U-JB-04 | 내 Job 목록 | 상태/파티션/기간 필터, Job ID·이름 검색 | squeue, sacct | 필수 |
| U-JB-05 | Job 상세 조회 | 상태, 할당 노드, 자원 사용량, 제출 스크립트, 작업 디렉토리 | scontrol show job, sstat | 필수 |
| U-JB-06 | 실시간 로그 조회 | stdout/stderr 실시간 tail 뷰 | 파일 스트리밍 | 필수 |
| U-JB-07 | Job 취소 | 단일/다중 Job 취소 | scancel | 필수 |
| U-JB-08 | Job 재제출 | 완료/실패 Job을 동일 설정으로 재제출(설정 수정 가능) | sbatch | 필수 |
| U-JB-09 | Job 이력 조회 | 기간별 완료 Job 이력, 자원 사용량(CPU시간, 메모리 peak), 종료 코드 | sacct | 필수 |
| U-JB-10 | 대기 사유 확인 | Pending Job의 사유(Priority, Resources, QOS limit 등) 표시 | squeue -o %r | 권장 |
| U-JB-11 | Job 알림 | Job 시작/완료/실패 시 이메일 또는 포털 알림 | Slurm mail / Portal | 권장 |
| U-JB-12 | 예상 시작 시간 | Pending Job의 예상 시작 시간 표시 | squeue --start | 선택 |

### 2.3 인터랙티브 앱 (Interactive Apps)

| ID | 기능명 | 설명 | 관련 기술 | 우선순위 |
|---|---|---|---|---|
| U-IA-01 | Jupyter 세션 | 자원 지정 후 JupyterLab 세션 실행, 브라우저 접속 | sbatch + 리버스 프록시 | 필수 |
| U-IA-02 | 원격 데스크톱 | Linux 데스크톱(noVNC) 세션 실행 | sbatch + VNC/noVNC | 권장 |
| U-IA-03 | VS Code Server | 웹 기반 코드 편집 세션 | code-server | 권장 |
| U-IA-04 | 세션 관리 | 실행 중 세션 목록, 재접속, 종료 | Portal DB + scancel | 필수 |
| U-IA-05 | 세션 공유 | View-only 링크로 세션 화면 공유 | 프록시 토큰 | 선택 |

### 2.4 파일 관리

| ID | 기능명 | 설명 | 우선순위 |
|---|---|---|---|
| U-FM-01 | 파일 브라우저 | 홈/스크래치/그룹 디렉토리 탐색, 정렬/검색 | 필수 |
| U-FM-02 | 업로드/다운로드 | 로그인 노드 경유 SFTP 방식 업/다운로드 (웹 직접 전송 미사용) | 필수 |
| U-FM-03 | 파일 편집 | 웹 텍스트 에디터(문법 하이라이팅) | 필수 |
| U-FM-04 | 파일 조작 | 복사/이동/삭제/이름변경/권한변경, 압축/해제 | 필수 |

### 2.5 셸 접근

| ID | 기능명 | 설명 | 우선순위 |
|---|---|---|---|
| U-SH-01 | 웹 터미널 | 브라우저 내 로그인 노드 셸 접속 | 권장 |
| U-SH-02 | 노드 접속 | 본인 Job이 실행 중인 컴퓨트 노드 셸 접속 | 선택 |

### 2.6 사용량/계정

| ID | 기능명 | 설명 | 관련 명령 | 우선순위 |
|---|---|---|---|---|
| U-AC-01 | 내 사용량 통계 | 기간별 CPU/GPU 시간, Job 수 차트 | sreport, sacct | 필수 |
| U-AC-02 | Fairshare/할당량 | 계정별 fairshare, QOS 한도 및 잔여량 | sshare, sacctmgr | 권장 |
| U-AC-03 | 프로필 관리 | 알림 설정, SSH 공개키 등록 | Portal DB | 권장 |
| U-AC-04 | 지원 요청 | 헬프데스크 티켓 제출(Job ID 자동 첨부) | 티켓 시스템 연동 | 선택 |

---

## 3. 관리자(ADMIN) 기능 정의

### 3.1 통합 모니터링 대시보드

| ID | 기능명 | 설명 | 관련 Slurm 명령/API | 우선순위 |
|---|---|---|---|---|
| A-DB-01 | 클러스터 전체 현황 | 노드 상태 맵(idle/alloc/mix/down/drain), 전체 사용률 | sinfo, scontrol | 필수 |
| A-DB-02 | 실시간 부하 현황 | CPU/GPU/메모리/네트워크 사용률 시계열 차트 | Prometheus/Ganglia 연동 | 필수 |
| A-DB-03 | 큐 현황 | 파티션별 Running/Pending Job 수, 평균 대기시간 | squeue | 필수 |
| A-DB-04 | 장애 알림 | 노드 down/drain 발생, 스케줄러 이상 시 알림 | slurmctld 이벤트 | 필수 |
| A-DB-05 | 스토리지 현황 | 파일시스템별 사용률, 사용자별 top 사용량 | df, quota | 권장 |

### 3.2 노드/파티션 관리

| ID | 기능명 | 설명 | 관련 명령 | 우선순위 |
|---|---|---|---|---|
| A-ND-01 | 노드 상태 제어 | drain/resume/down 처리, 사유(reason) 입력 | scontrol update node | 필수 |
| A-ND-02 | 노드 상세 조회 | 스펙(CPU/GPU/메모리), 실행 중 Job, 상태 이력 | scontrol show node | 필수 |
| A-ND-03 | 파티션 관리 | 파티션 생성/수정(노드 구성, 시간제한, 우선순위, 접근권한) | scontrol, slurm.conf | 필수 |
| A-ND-04 | 예약(Reservation) | 특정 기간 노드 예약 생성/삭제(점검, 특정 그룹 전용) | scontrol create reservation | 권장 |
| A-ND-05 | 점검 모드 | 공지 등록 + 대상 노드 일괄 drain 워크플로 | scontrol + Portal | 선택 |

### 3.3 Job 관리 (전체)

| ID | 기능명 | 설명 | 관련 명령 | 우선순위 |
|---|---|---|---|---|
| A-JB-01 | 전체 Job 조회 | 모든 사용자 Job 목록, 사용자/파티션/상태 필터 | squeue, sacct | 필수 |
| A-JB-02 | Job 강제 제어 | 임의 Job 취소/보류(hold)/해제(release) | scancel, scontrol | 필수 |
| A-JB-03 | 우선순위 조정 | 특정 Job 우선순위 변경(top 처리) | scontrol update job | 권장 |
| A-JB-04 | 장기 점유 감지 | 비정상 장기 실행/유휴 Job 탐지 리포트 | sacct + 분석 | 선택 |

### 3.4 사용자/계정/정책 관리

| ID | 기능명 | 설명 | 관련 명령 | 우선순위 |
|---|---|---|---|---|
| A-US-01 | 사용자 관리 | AD(단일) 연동 자동 프로비저닝 — 허용 그룹·식별 속성(uid/cn) 조건에 맞는 사용자를 동기화 시 자동 활성화(별도 가입 승인 없음), 역할(USER/ADMIN) 부여·비활성화. 역할은 포털 authZ 테이블에서 관리(AD 그룹/스키마 미변경), 최초 ADMIN은 최초 실행 시 최초 AD 연결 설정 화면(1회용 토큰 보호)에서 AD 검증 후 그 AD 사용자 1명 seed. 포털에서 계정을 직접 생성하지 않음 | LDAP/AD 연동, sacctmgr | 필수 |
| A-US-02 | 계정(Account) 관리 | 클러스터별 독립 slurmdbd에서 Slurm 계정 생성/수정/삭제, AD 사용자를 계정에 N:M 매핑(join), 계정별 허용/기본 QOS·Fairshare share 배정(소속 사용자 상속) | sacctmgr | 필수 |
| A-US-03 | QOS 관리 | 클러스터별 QOS 생성/수정(최대 Job 수, 자원 한도, 우선순위) · 계정/사용자에 허용·기본 QOS 배정 | sacctmgr | 필수 |
| A-US-04 | Fairshare 설정 | 클러스터별 계정 share 값 설정(계정 관리 화면에 통합) | sacctmgr | 권장 |
| A-US-05 | 접근 제어 | 파티션별 허용 계정/그룹 설정 | slurm.conf, AllowAccounts | 권장 || A-US-06 | 자원 신청 승인 워크플로 | 자원 증설·QOS 등 자원 신청 승인 프로세스. 사용자 가입은 AD 자동 프로비저닝으로 처리되어 별도 승인 없음(A-US-01) | Portal DB | 선택 |

### 3.5 통계/리포팅

| ID | 기능명 | 설명 | 관련 명령 | 우선순위 |
|---|---|---|---|---|
| A-RP-01 | 사용량 리포트 | 기간별·사용자별·계정별 CPU/GPU 시간, Job 수 통계 | sreport | 필수 |
| A-RP-02 | 사용률 분석 | 클러스터 가동률, 파티션별 사용률 추이 | sreport, 모니터링 DB | 필수 |
| A-RP-03 | 대기시간 분석 | 파티션/QOS별 평균 대기시간 추이 | sacct 분석 | 권장 |
| A-RP-04 | 리포트 내보내기 | CSV/Excel/PDF 내보내기, 정기 리포트 메일 발송 | Portal | 권장 |
| A-RP-05 | 과금 연계 | 사용량 기반 과금 데이터 산출 | sreport + 요율 | 선택 |

### 3.6 포털 운영 관리

| ID | 기능명 | 설명 | 우선순위 |
|---|---|---|---|
| A-OP-01 | 공지사항 관리 | 공지 등록/수정/삭제, 배너 노출 설정 | 필수 |
| A-OP-02 | 앱/템플릿 관리 | Job 템플릿·인터랙티브 앱 등록, 버전/파라미터 정의 | 필수 |
| A-OP-03 | 감사 로그(Audit) | 포털 내 주요 행위(제출/취소/설정 변경) 로그 조회 | 필수 |
| A-OP-04 | 시스템 설정 | 세션 정책(폴링 주기·세션 타임아웃)·알림 채널(SMTP/웹훅) 설정. 인증(AD)은 AD 연결(A-US-01), Slurm 엔드포인트는 클러스터 관리에서 관리하며 2FA는 AD/IdP 위임 | 권장 |
| A-OP-05 | 헬프데스크 관리 | 사용자 지원 티켓 조회/응답 | 선택 |

### 3.7 License 관리

| ID | 기능명 | 설명 | 관련 명령/API | 우선순위 |
|---|---|---|---|---|
| A-LM-01 | 라이선스 서버 등록 | 외부 FlexNet Publisher/FlexLM 서버 연결 설정(port@host, 벤더 데몬), 연결 테스트 | lmutil lmstat | 필수 |
| A-LM-02 | Feature 사용 현황 수집 | 주기적 lmstat 수집으로 Feature별 총/사용/가용 수량 조회 | lmutil lmstat -a | 필수 |
| A-LM-03 | Slurm Licenses 동기화 | 수집한 가용 수량을 Slurm license 리소스로 반영(Job 제출 시 --licenses 예약) | sacctmgr add resource | 권장 |
| A-LM-04 | 부족 알림 | 가용률 임계치 미만 Feature 발생 시 관리자 알림 | Portal | 선택 |
| A-LM-05 | 사용 모니터링 | Feature별 체크아웃 현황(사용자·클러스터·Job·기간)을 Slurm Job과 매핑하여 조회 | lmutil lmstat -a + squeue | 권장 |

---

## 4. 공통/비기능 요구사항

| ID | 항목 | 내용 |
|---|---|---|
| C-01 | 인증 | SSO(OIDC/SAML) 또는 LDAP 연동, 관리자 2FA 권장 |
| C-02 | 권한 | RBAC 기반 메뉴/기능 접근 제어 (USER/ADMIN). 인증은 AD, 역할은 포털 authZ 테이블(`ad_id → role`)에서 관리 — AD 그룹/스키마 미변경, 미러 동기화 없음. 최초 ADMIN은 **최초 실행 시 최초 AD 연결 설정 화면**(1회용 setup 토큰·로컬 접근으로 보호)에서 AD 연결·검증 후 그 AD 사용자 1명을 seed ADMIN으로 지정(로컬 비밀번호 미저장), 이후 이 seed 영역 비표시 |
| C-03 | 연동 방식 | **slurmrestd(REST) 주 경로 + SSH/SFTP 보조 경로** 하이브리드 — 구조화 조회·제어는 REST, 파일시스템·PTY·인터랙티브·스트리밍은 SSH/SFTP, REST 미지원 op만 CLI 래핑. 기능별 경로 매핑은 §4.1 |
| C-04 | 실시간성 | Job 상태/노드 상태 폴링 주기 ≤ 30초, 로그는 스트리밍 |
| C-05 | 감사/보안 | 모든 제어성 액션(제출/취소/노드 제어) 감사 로그 기록 |
| C-06 | 다국어 | 한국어/영어 지원 |
| C-07 | 반응형 UI | 데스크톱 우선, 태블릿 대응 |

### 4.1 Slurm 연동 경로 매핑 (C-03 상세)

포털은 **slurmrestd(REST)를 주 경로**, **SSH/SFTP를 보조 경로**로 사용한다. 원칙: 구조화된 조회·제어는 REST(무상태·JSON·JWT 사용자 귀속·주입 표면 없음), 파일시스템·PTY·인터랙티브·스트리밍처럼 REST가 다루지 못하는 것은 SSH/SFTP.

**① REST (slurmrestd) — 주 경로 · 구조화 조회/제어**
- 클러스터·파티션 현황: U-CL-01·02, A-DB-01·03
- Job 제출/조회/제어: U-JB-01·02·03·04·05·07·08·09·10·12, A-JB-01·02·03, U-IA-04(세션 종료 `scancel`; 세션 메타는 Portal DB)
- 노드·파티션·예약: A-ND-01·02·04 (A-ND-03 파티션은 REST update, `slurm.conf` 반영은 구성관리/CLI)
- 계정·QOS·association·사용자: A-US-02·03·04 (※ 일부 `sacctmgr` op은 목표 slurmrestd 버전 미지원 시 해당 op만 CLI 래핑)
- 사용량·Fairshare: U-AC-01·02

**② SSH/SFTP — 보조 경로 · 파일시스템/PTY/인터랙티브/스트리밍**
- 파일 관리: U-FM-01~04 (로그인 노드 경유 SFTP)
- 셸 접속: U-SH-01·02 (SSH PTY)
- 인터랙티브 앱 세션: U-IA-01·02·03·05 — Job 제출 자체는 REST(`sbatch`), **세션 연결·noVNC/code-server 리버스 프록시**는 SSH/프록시
- 실시간 로그 tail: U-JB-06 (파일 스트리밍), U-JB-02 스크립트 업로드분(SFTP)
- REST 미지원 op 한정 CLI 래핑
- **접속 방식**: 클러스터 등록 시 지정한 로그인 노드(웹 터미널·SFTP 공용, 별도 DTN 미사용)에 SSH(기본 포트 22)로 접속. **포털 서비스 계정 + SSH 개인키(Secret 저장)** 사용, **제한적 sudo(least privilege)로 사용자 impersonation** — 전체 root sudo 지양
- **경로 인식** (U-FM-01 바로가기): 홈은 SSSD/NSS(`getent passwd`·`$HOME`)로 **사용자별 자동 인식**(설정 안 받음), 그룹·스크래치는 클러스터 등록 시 **경로 템플릿**(`{user}`·`{group}` 세션 치환)으로 지정

**③ 기타 연동 (Slurm 외)**
- 메트릭: A-DB-02(Prometheus/Ganglia), A-DB-05(df/quota)
- 이벤트/알림: A-DB-04(slurmctld 이벤트), U-JB-11(Slurm mail/Portal)
- Portal DB: U-CL-03 공지, U-JB-03 템플릿, U-IA-04 세션, U-AC-03·04 프로필/티켓, C-02 역할(authZ)

> 구현 유의: slurmrestd는 `data_parser`/API 버전(v0.0.x)이 deprecate되므로 목표 버전(현재 v0.0.41 상정)에 고정하고 REST 호출을 어댑터 계층 뒤에 둔다. slurmrestd는 네트워크 직접 노출 금지 — 포털 백엔드만 접근하고 사용자 AD 인증 → Slurm username 매핑 → 스코프된 JWT로 호출(감사 로그 C-05 연계).

---

## 5. 화면 목록 (Screen Inventory)

| 화면 ID | 화면명 | 역할 | 매핑 기능 |
|---|---|---|---|
| SCR-01 | 로그인 | 공통 | C-01 |
| SCR-02 | 클러스터 | USER | U-CL-01~02 |
| SCR-03 | Job 목록 | USER | U-JB-04, 07~09 |
| SCR-04 | Job 제출 (폼/스크립트/템플릿) | USER | U-JB-01~03 |
| SCR-05 | Job 상세 (로그 포함) | USER | U-JB-05, 06, 10 |
| SCR-06 | 인터랙티브 앱 런처/세션 | USER | U-IA-01~05 |
| SCR-07 | 파일 관리자 | USER | U-FM-01~04 |
| SCR-08 | 웹 터미널 | USER | U-SH-01~02 |
| SCR-09 | 내 사용량/프로필 | USER | U-AC-01~04 |
| SCR-10 | 관리자 대시보드 | ADMIN | A-DB-01~05 |
| SCR-11 | 노드/파티션 관리 | ADMIN | A-ND-01~05 |
| SCR-12 | 전체 Job 관리 | ADMIN | A-JB-01~04 |
| SCR-13 | 사용자/AD 연결/계정/QOS 관리 (클러스터별) | ADMIN | A-US-01~06 |
| SCR-14 | 통계/리포트 | ADMIN | A-RP-01~05 |
| SCR-15 | 포털 운영 설정 | ADMIN | A-OP-01~05 |
| SCR-16 | 공지사항 목록 | USER | U-CL-03 |
| SCR-17 | License 관리 | ADMIN | A-LM-01~05 |

---

## 6. 우선순위 기준

- **필수**: MVP(1차 릴리스) 포함
- **권장**: 2차 릴리스 목표
- **선택**: 백로그, 수요 확인 후 결정