apiVersion: v2
name: hpc-portal
description: Slurm HPC 클러스터 웹 포털 (FastAPI + Vue) — MySQL·Redis 포함
type: application
# 차트 자체의 판. 앱 판올림과 따로 움직인다.
version: 0.1.0
# 기본 이미지 태그. values.image.*.tag가 비면 이 값을 쓴다.
appVersion: "0.1.0"
keywords: [slurm, hpc, portal]
